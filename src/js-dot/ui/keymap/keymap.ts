/**
 * Keymap Operation
 */
export class Keymap {

	/**
	 * Set key
	 * @param keys
	 * @param callback
	 */
	set(keys: string, callback: Function) {

	}

	/**
	 * Sey key per once.
	 * @param keys
	 * @param callback
	 */
	once(keys: string, callback: Function) {

	}
}

export class KeymapListener {
	constructor(callback: Function) {

	}

	detach(){

	}
}