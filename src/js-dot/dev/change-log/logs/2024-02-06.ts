export default `
# 2024-02-06

* Bundle files are split, If have chunk files.
* RelyToken have sugar methods.
	- RelyToken.rely same as rely(RelyToken)
	- RelyToken.reliabled same as reliabled(RelyToken)
	- RelyToken.reliably same as reliably(RelyToken)
* relyify() accept multiple token as array.
* inject() removed as expired compatibles for deprecates.
`;