export default `
# 2024-02-06

rely* or reli* are compound word for family of rely() functions. even somethings are not in dictionary.
But you can understand meaning of it.

* relyify()
	- Has a alias as relify()
	- Add .override property to option, Can replace rely tokens when true even It rely-ify-ed already.
* reliabled()
	- Has a alias as relyabled()
	- No longer returns token anymore when it rely-able-ed, returns true.
* reliably() has a alias as relyably()
* @Reliable
 	- Has as alias as Relyable()
`;